// This template requires dishes array to be set in $scope
//

extends ../component-layout

block content
    div(ng-controller='MealsController', class='container')

        include ../top

        ul(id='id-dishes-list')
            li(ng-repeat='dish in meals')
                a(href='dishes/view.html?id={{dish._id}}') {{dish.name}}
                p {{dish.description}}
include ../footer
