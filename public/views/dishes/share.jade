extends ../component-layout

block content
    div(ng-controller='DishesShareController', class='container')

        include ../top

        div(class='centered-container')
            button(class='btn btn-success', ng-click='sendSms()')
                span(class='glyphicon glyphicon-envelope button-icon')
                {{lang.notifythem}}
            button(class='btn btn-default', ng-click='skip()')
                span(class='glyphicon glyphicon-arrow-right button-icon')
                {{lang.skip}}
        div(id='id-contacts', class='list-group')
            a(id='{{contact.mobilePhone}}', ng-repeat='contact in contacts | orderBy: "displayName"', class='list-group-item contact') {{contact.displayName}}

include ../footer
