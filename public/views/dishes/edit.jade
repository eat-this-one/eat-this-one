extends ../layout

block configuration
    - var controllerName = "DishesEditController"

block base
    base(href='../')

block logic
    form(role='form' id='id-page-form')
        eat-photo(element='photo')
        eat-input-text(element='name')
        eat-input-text(element='description')
        eat-select(element='when')
        eat-select(element='nportions')
        eat-select(element='donation')
